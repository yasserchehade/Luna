---
status: accepted
---

# Keep originals in a user-owned readable cabinet

Luna will file untouched original documents into ordinary human-readable folders chosen by the household, while extracted text and richer relationships remain supplemental Luna knowledge. We rejected an opaque Luna-managed vault because the household must retain usable records if Luna is removed, accepting that safe filesystem operations and external sync behaviour require more care.
