{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for Luna's main desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
