{
  "build": {
    "beforeBuildCommand": "pnpm typecheck && pnpm exec vite build --mode live-canary"
  },
  "app": {
    "security": {
      "csp": null,
      "capabilities": [
        "default",
        {
          "identifier": "live-canary",
          "description": "Local-only permissions for Luna's isolated managed-access canary",
          "windows": ["main"],
          "permissions": [
            "wdio:default",
            "wdio-webdriver:default"
          ]
        }
      ]
    }
  }
}
